/** This map is for the retrievProfileDeatils method of AthleteService,
 * where a user gives data to query and the method is built through the query input.
 * The properties correspond to the table info requested and the value is the
 * actual table name.
 * 
 */
export const ATHLETE_RELATIONS_MAPPINGS = {
    'federation': 'federations',
    'division': 'divisions',
    'weight_class': 'weight_classes'
}